/** Must match the `name` of the widget entry that app.json's react-native-android-widget config
 * plugin registers (see the final report for the exact config that plugin still needs — it cannot
 * be added here, since app.json is off-limits for this task). */
export const WIDGET_NAME = 'NextTask';
